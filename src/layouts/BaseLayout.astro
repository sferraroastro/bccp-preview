---
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
}

const { title, description = "Berkeley Center for Cosmological Physics" } = Astro.props;
const nav = [
  { href: "/", label: "Home" },
  { href: "/research/", label: "Research" },
  { href: "/people/", label: "People" },
  { href: "/events/", label: "Events" },
  { href: "/jobs/", label: "Jobs" },
  { href: "/visiting/", label: "Visiting" },
  { href: "/donate/", label: "Donate" }
];
const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const withBase = (path: string) => `${base}${path}`;
const normalizedCurrentPath = currentPath.startsWith(`${base}/`)
  ? currentPath.slice(base.length) || "/"
  : currentPath;
const isCurrentPage = (href: string) => href === "/" ? normalizedCurrentPath === "/" : normalizedCurrentPath === href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title} | BCCP</title>
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to main content</a>
    <header class="site-header">
      <div class="header-inner">
        <a class="brand" href={withBase("/")} aria-label="BCCP home">
          <img src={withBase("/images/logo_108.png")} alt="" />
        </a>
        <nav class="nav" aria-label="Primary navigation">
          {nav.map((item) => (
            <a href={withBase(item.href)} aria-current={isCurrentPage(item.href) ? "page" : undefined}>
              {item.label}
            </a>
          ))}
        </nav>
      </div>
    </header>
    <main class="main" id="main-content">
      <slot />
    </main>
    <footer class="site-footer">
      <div class="footer-inner">
        <span>Contents © 2026 BCCP</span>
        <span>
          Berkeley Center for Cosmological Physics
          <a href="https://dap.berkeley.edu/report-web-accessibility-issue">Accessibility</a>
        </span>
      </div>
    </footer>
  </body>
</html>
